[toolchain]
anchor_version = "0.31.1"

[features]
resolution = true
skip-lint = false

[programs.devnet]
flamingolive_escrow = "BcEopLQ9MxMdMtU57m5KYA4sk9qvhy29XkneEKHcfuSf"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
devnet-test = "npx ts-mocha -p tsconfig.json -t 1000000 tests/devnet_live_test.ts"
test = "npx ts-mocha -p tsconfig.json -t 1000000 tests/**/*.ts"
